module.exports = class Lobby{
    constructor(client){
        // Check if players are on server
        // Create channel
        // Invite/give permissions to players
        // Wait to accept enemy
        // Create board
        // Run board
    }
    messageCreate(msg, args){

    }
    messageReactionAdd(msg, emoji, userID, client) {
        if(msg.author.id)
    }
}