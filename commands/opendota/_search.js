const { Command } = require('aghanim')

module.exports = new Command('search',{
  category : 'Dota 2', help : 'Búsqueda de pros', args : '<player/pro> <búsqueda>'},
  function(msg, args, command){
    // let self = this
  })
